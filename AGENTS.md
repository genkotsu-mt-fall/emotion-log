# Agent entry point

このリポジトリでは、リポジトリ内の文書を正本として扱う。

## 作業開始

1. `docs/state.md`を読み、マージ済みの現在地を確認する。
2. `docs/skills/000-workflow.md`を読み、現在の作業フローに従う。
3. `docs/state.md`の`current.path`が指す現在タスクを読む。
4. 実装方針、新規コード、既存コード変更、リファクタリングを含む場合だけ、`docs/engineering/code-constraints.md`も読む。
5. AIDDワークフロー自体の発展方針を扱う場合は、`docs/aidd/evolution-strategy.md`を読む。

必要な文書だけを追加で確認し、最初からリポジトリ内の全ドキュメントを読み込まない。

## 判断の境界

- `docs/state.md`にないチャット中の案や未マージの変更を、適用済みの事実として扱わない。
- ユーザーが確定していないプロダクト判断、アーキテクチャ判断、UX判断を新しく確定しない。
- 現在タスクを進めるために新しい判断が必要になった場合は、既存のOQ、ADR、要件、タスクを確認し、それでも未決定なら未決定事項として明示する。
- タスクの対象外を、便利そうという理由だけで追加実装しない。
- 共通ルールとタスク固有ルールが競合する場合は、`docs/skills/000-workflow.md`に従って例外の理由と範囲を明示する。

## 変更と検証

- `main`へ直接変更せず、ブランチとPull Requestを使う。
- タスク文書に指定された検証を実行する。
- コード変更を含む場合は、`docs/engineering/code-constraints.md`に記載された検証条件も確認する。
- 変更後は少なくとも`git diff --check`を実行する。
- 検証できなかった項目を成功扱いにしない。

このファイルは各エージェント向けの詳細ルールを複製する場所ではなく、正本へ到達するための共通入口として保つ。
